# VestLab Finance News — 前端设计文档

## 产品定位

面向中国投资者的**全球财经新闻仪表盘**。整合 AI 每日市场简报、全球指数行情、双语新闻流三大核心功能。

---

## 技术栈

| 项目 | 选型 | 理由 |
|---|---|---|
| 框架 | **Vite + React + TypeScript** | 轻量快速，适合 SPA |
| 样式 | **Tailwind CSS 4.x** | 原子化 CSS，暗色主题原生支持 |
| 组件库 | **shadcn/ui** | 可定制、无 vendor lock-in、基于 Radix |
| 部署 | **Cloudflare Pages** | 与 Workers 后端同生态 |
| 字体 | **Inter** (英文) + **Noto Sans SC** (中文) | 专业美观 |
| Markdown | **react-markdown** | 渲染 AI 简报 |

---

## 页面结构

```
┌──────────────────────────────────────────────────────┐
│  🔴 Market Ticker Bar（指数滚动条）                    │
│  SPX 6142 ▲0.82%  IXIC 19834 ▲1.21%  N225 38920 ...  │
├──────────────────────────┬───────────────────────────┤
│                          │                           │
│   每日市场简报            │     新闻列表              │
│   (Daily Briefing)       │     (News Feed)           │
│                          │                           │
│   📅 日期选择器           │   🔍 搜索 / 筛选源        │
│                          │                           │
│   ┌─────────────────┐    │   ┌─────────────────────┐ │
│   │ AI Markdown 报告 │    │   │ 📰 新闻卡片          │ │
│   │                 │    │   │ [Bloomberg] 14:30    │ │
│   │ ## 📊 市场脉搏  │    │   │ 中文标题              │ │
│   │ ## 🔥 焦点事件  │    │   │ English subtitle     │ │
│   │ ## 📈 资产联动  │    │   ├─────────────────────┤ │
│   │ ## 🌍 地缘政策  │    │   │ 📰 新闻卡片          │ │
│   │ ## 🔮 明日关注  │    │   │ [WSJ Markets] 13:15  │ │
│   │                 │    │   │ 中文标题              │ │
│   └─────────────────┘    │   │ English subtitle     │ │
│                          │   └─────────────────────┘ │
├──────────────────────────┴───────────────────────────┤
│  Footer: VestLab © 2026 | Data: Yahoo Finance        │
└──────────────────────────────────────────────────────┘
```

---

## 三大核心模块

### 1. Market Ticker Bar（指数滚动条）

**位置**：页面顶部，固定不滚动

**数据源**：`GET /api/market-data`

**展示内容**：
- 每个指数显示：名称、当前价、涨跌幅
- 涨为绿色 ▲，跌为红色 ▼
- 水平排列，超出部分自动滚动（CSS animation）

**设计要点**：
- 深色背景（`#0f172a`），半透明毛玻璃效果
- 数字使用等宽字体（tabular-nums）
- 每 15 分钟自动刷新（与后端 cron 同步）

---

### 2. Daily Briefing（每日市场简报）

**位置**：左侧主区域（桌面 60%，移动端全宽）

**数据源**：`GET /api/daily-summary?date=YYYY-MM-DD`

**功能**：
- 日期选择器：可切换查看历史简报
- 使用 `react-markdown` 渲染 AI 报告（支持标题、粗体、列表、emoji）
- 无简报时显示优雅的空状态

**设计要点**：
- 卡片容器，圆角 12px，微妙阴影
- Markdown 内的 `##` 标题加左侧彩色竖条装饰
- 报告底部显示时间戳和 AI 标识

---

### 3. News Feed（新闻列表）

**位置**：右侧边栏（桌面 40%，移动端全宽折叠）

**数据源**：`GET /api/news?limit=50`

**每条新闻卡片**：
- 来源徽章：Bloomberg（紫色）/ WSJ Markets（金色）/ WSJ Economy（蓝色）/ WSJ World（绿色）
- 中文翻译标题（主标题）
- 英文原标题（副标题，灰色小字）
- 发布时间（相对时间："2 小时前"）
- 点击跳转原文链接

**筛选功能**：
- 按来源筛选（全部 / Bloomberg / WSJ Markets / WSJ Economy / WSJ World）
- 搜索框（搜索标题关键词）

---

## 设计规范

### 色彩系统（暗色主题 + shadcn/ui）

shadcn/ui 默认使用 CSS 变量 + HSL 色彩。在 `src/index.css` 中通过 `@theme` 扩展：

```css
@import "tailwindcss";

@theme {
  /* 涨跌色 */
  --color-up: oklch(0.72 0.19 142);       /* 绿 */
  --color-down: oklch(0.64 0.21 25);      /* 红 */

  /* 来源徽章 */
  --color-badge-bloomberg: oklch(0.59 0.2 293);   /* 紫 */
  --color-badge-wsj-market: oklch(0.77 0.16 70);  /* 金 */
  --color-badge-wsj-economy: oklch(0.62 0.17 256);/* 蓝 */
  --color-badge-wsj-world: oklch(0.7 0.15 165);   /* 绿 */
}
```

> shadcn/ui 的 `--background`、`--card`、`--foreground` 等变量自动处理暗色主题，无需手动配置。
> 使用 `npx shadcn@latest init` 时选择 **Neutral** base color + **dark mode**。

### 响应式断点

| 断点 | 宽度 | 布局 |
|---|---|---|
| 桌面 | ≥1024px | 双栏（简报 60% + 新闻 40%） |
| 平板 | 768-1023px | 单栏堆叠 |
| 手机 | <768px | 单栏，Ticker 精简 |

---

## API 集成

```typescript
const API_BASE = 'https://vestlab-finance-news.digitalboyzone.workers.dev';

// 1. 获取市场指数
const marketData = await fetch(`${API_BASE}/api/market-data`).then(r => r.json());
// { count: 12, data: [ { symbol, name, price, change_percent, ... } ] }

// 2. 获取每日简报
const summary = await fetch(`${API_BASE}/api/daily-summary?date=2026-02-13`).then(r => r.json());
// { date: '2026-02-13', summary: '# VestLab 财经新闻综述...' }

// 3. 获取新闻列表
const news = await fetch(`${API_BASE}/api/news?limit=50`).then(r => r.json());
// [ { id, source, title, translated_title, translated_content, published_at, url, ... } ]
```

---

## 目录结构（计划）

```
frontend/
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
├── tsconfig.app.json
├── components.json          ← shadcn/ui 配置
├── docs/
│   └── design.md            ← 本文档
├── public/
│   └── favicon.svg
└── src/
    ├── main.tsx
    ├── App.tsx
    ├── index.css              ← Tailwind + shadcn 主题变量
    ├── components/
    │   ├── ui/                ← shadcn/ui 组件（自动生成）
    │   │   ├── button.tsx
    │   │   ├── card.tsx
    │   │   ├── badge.tsx
    │   │   ├── input.tsx
    │   │   ├── select.tsx
    │   │   ├── scroll-area.tsx
    │   │   └── separator.tsx
    │   ├── MarketTicker.tsx    ← 指数滚动条
    │   ├── DailyBriefing.tsx   ← 每日简报
    │   ├── NewsFeed.tsx        ← 新闻列表
    │   └── NewsCard.tsx        ← 新闻卡片
    ├── hooks/
    │   └── useApi.ts           ← API 调用 hooks
    └── lib/
        ├── api.ts              ← API client
        ├── types.ts            ← 类型定义
        └── utils.ts            ← cn() 等工具函数（shadcn）
```
